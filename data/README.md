# Runtime data

This folder is mounted into the Docker container for local SQLite storage.
Do not commit real database files because they can contain Telegram user IDs, group IDs, answers, grades, and other private data.
